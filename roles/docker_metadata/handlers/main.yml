---
- name: restart nginx
  service:
    name: nginx
    state: restarted

- name: "systemd daemon-reload"
  systemd:
    daemon_reload: true

- name: "restart zabbix-agent"
  systemd:
    name: "zabbix-agent2.service"
    state: "restarted"
