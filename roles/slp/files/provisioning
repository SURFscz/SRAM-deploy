/var/log/slp.log {
        weekly

        rotate 24
        dateext
        missingok
        copytruncate
        olddir /var/log/slp
        createtolddir 0750 root adm
        compress
        delaycompress
}
