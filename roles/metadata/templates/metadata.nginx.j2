server {
        listen {{ meta_port }} default_server;
        listen [::]:{{ meta_port }} default_server;

        root {{ metadata_documentroot }};

        location / {
                autoindex on;
        }
}
