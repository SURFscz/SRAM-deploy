{% set lb_ldap = loadbalancer|selectattr("protocol", "equalto", "ldap")|list %}
#!/bin/bash
{% for host in lb_ldap %}
{% set name = host.hostname.split('.')[0] -%}
echo "enable frontend ldap" | socat stdio /run/haproxy/admin.sock
{% endfor %}
