---
- name: "systemd daemon-reload"
  systemd:
    daemon_reload: true

- name: "restart haproxy"
  listen: "cert updated"
  systemd:
    name: "haproxy"
    state: "restarted"
    enabled: true

- name: "restart fence"
  systemd:
    name: "fence"
    state: "restarted"
    enabled: true
