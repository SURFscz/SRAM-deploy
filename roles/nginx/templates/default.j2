server {
        listen {{default_port}} default_server;
        listen [::]:{{default_port}} default_server;

        root /var/www/html;

        include security_headers;

        location / {
           return 403;
        }
}
