# certbot config

{% if letsencrypt_staging %}
test-cert
{% endif %}

email={{mail.admin_address}}

agree-tos
max-log-backups 10

rsa-key-size 2048

authenticator = webroot
webroot-path {{letsencrypt_challenge_dir}}

deploy-hook = /bin/bash -c 'cd "$RENEWED_LINEAGE" && cat privkey.pem fullchain.pem > priv+fullchain.pem'

