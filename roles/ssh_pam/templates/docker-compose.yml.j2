# vim:ft=yaml
---
version: '3.3'

services:
  ssh_sandbox:
    image: "surfscz/pam-websso:v1"
    ports:
      - "2022:22"
    volumes:
      - "{{pam_config_dir}}:/opt/config:ro"
    restart: always

